import "../App.css";

function RoundCalculator() {
  return (
    <main className="app">
      <section className="calculator-card">
        <p className="calculator-eyebrow">Game Start</p>
        <h2 className="calculator-title">라운드 점수 계산기를 만들 준비가 되었습니다.</h2>
        <p className="calculator-description">
          다음 단계에서는 여기서 순위, 티츄 선언, 트릭 점수를 입력하는 첫 번째 라운드 계산기를
          붙이면 됩니다.
        </p>
      </section>
    </main>
  );
}

export default RoundCalculator;
